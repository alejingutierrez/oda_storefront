# Backlog

## Now (F0 – bootstrap E2E mínimo)
- MC-005 | todo | P0 | Primer scraper E2E | Descubrir sitemap marca piloto, parsear, enviar a GPT-5.1, upsert DB, ficha en VSF + ISR.
- MC-007 | todo | P1 | CI lint/test smoke | GitHub Actions: lint, type-check, unit smoke; build base.
- MC-008 | todo | P1 | Observabilidad mínima | Logging estructurado, health checks, traces básicos; dashboard inicial de servicios.

## Next (F1 – primeras capacidades)
- MC-010 | todo | P1 | Búsqueda básica + pgvector | Index texto/embeddings, endpoint search, listados VSF, métricas de calidad.
- MC-011 | todo | P1 | Observabilidad scraping v1 | Métricas por marca, staleness, alertas básicas, dashboard.
- MC-012 | todo | P2 | Admin mínimo | Login, CRUD marcas/tiendas, salud scraper, aprobación manual de normalizaciones IA.
- MC-013 | todo | P2 | Anuncios básicos | Modelo de placements, serving simple, tracking impresiones/clicks.
- MC-014 | todo | P1 | 10–20 marcas integradas | Parsers templated, scheduler adaptativo, cobertura inicial.
- MC-015 | todo | P2 | Emails y plantillas | SMTP/SendGrid, plantillas de servicio, opt-in/opt-out, logs de envío.
- MC-017 | todo | P1 | Gestión de secrets/entornos | Matriz local/stg/prod, rotación, docs de configuración.

## Later (F2–F3 – escalado y premium)
- MC-018 | todo | P1 | Recomendador híbrido v1 | Embeddings texto/imagen, kNN, reglas stock/talla, API de similares.
- MC-019 | todo | P1 | Recomendaciones proactivas + alertas | Back-in-stock, drops, email/push, control por plan.
- MC-020 | todo | P1 | Planes pagos Wompi | Checkout suscripción, webhooks, flags de features, facturación.
- MC-021 | todo | P2 | Try-on IA MVP | Upload seguro a Blob, pipeline async, expiración de assets, UX básica.
- MC-022 | todo | P2 | Panel advertiser | Campañas, budget, targeting simple, reporting CTR/CPA.
- MC-023 | todo | P2 | Versionado de prompts + FinOps IA | Costos por marca, límites diarios, dashboards de drift/error rate.
- MC-024 | todo | P1 | Escalado a 100+ marcas | Pool de proxies, crawler de respaldo, priorización por rotación/frescura.
- MC-025 | todo | P2 | Data quality & drift dashboard | Campos faltantes, duplicados, staleness, drift de modelo IA.
- MC-026 | todo | P2 | Gestión de colas y reintentos | Retries exponenciales, DLQ, priorización por marca/frescura.
- MC-027 | todo | P2 | Segmentación estilo/ocasión + A/B ranking | Experimentos, cohortes, métricas automáticas.
- MC-028 | todo | P2 | Store locator enriquecido | Geocodificación, horarios/teléfonos/redes actualizados, UX de mapa.
- MC-029 | todo | P2 | Seguridad y privacidad reforzada | WAF, rate limit afinado, borrado/anonimización try-on, auditoría completa.
- MC-030 | todo | P2 | Optimización de costos infra | Cache de inferencias sin cambios, compresión de assets, tuning colas/batch.
- MC-031 | todo | P2 | Despliegue prod Vercel + contenedores workers | Ramas stg/prod, CI/CD completo, monitoreo post-deploy.
- MC-032 | todo | P1 | Cobertura 500 marcas | Escalado scraping, acuerdos con marcas clave, SLAs de frescura.
- MC-033 | todo | P2 | Legal/compliance y políticas | Términos, privacidad, manejo de robots, procesos de takedown, retención de datos.
- MC-034 | todo | P2 | Performance & resiliencia front | Budgets CWV, fallbacks, manejo de errores de catálogo.

## Done (2026-02)
- MC-133 | done | P1 | PLPs fijas: precálculo semanal de insights de precio (SSR sin loading inicial) | `catalog-data` agrega `getCatalogPriceInsightsFixedPlp` con caché dedicada semanal (`CATALOG_FIXED_PLP_PRICE_INSIGHTS_REVALIDATE_SECONDS`, default `604800`) y reusa el cómputo completo de histograma/stats. `CatalogoView` detecta requests base de PLP fija y siembra SSR con histogram/chips listos (fallback robusto a `bounds` si falla). Se crea cron `/api/admin/catalog-price-insights/cron` (sharding por `path`, `slotCount` default `168`, `maxPaths` default `12`) y programación horaria en `apps/web/vercel.json`.
- MC-133 | done | P1 | PLP `/catalogo`: precio inicial determinista (SSR + revalidación cliente) | `CatalogoView` ahora siembra `initialPriceInsights` con bounds SSR para toda PLP compartida (`/catalogo`, `/femenino`, SEO dinámicas). `CatalogoFiltersPanel` corrige frescura de caché con `{value,cachedAt}` (legacy `cachedAt=0`), fuerza validación `mode=full` por key en mobile draft (`demanded` vs `networkValidated`), añade watchdog `12s` + reintentos acotados y alinea `min/max/step` del slider para evitar coerción inicial del browser.
- MC-149 | done | P1 | Aceleración y simplificación `/admin/real-style` (click-only) | El board de curación pasa a interacción click-only (sin drag/drop), conserva atajos `1..8` y agrega `Usar sugerido`. El frontend carga cartas por fast-path (`queue?includeSummary=false`) sin bloquear por resumen, hace prefetch por low-watermark, evita refetch completo al `Saltar` (skipped via `useRef` + `sessionStorage`) y mantiene resumen híbrido (background + actualización optimista + resync cada 10 asignaciones/manual). Backend: nueva ruta `GET /api/admin/real-style/summary`, `queue` y `assign` aceptan `includeSummary` opcional (default `true`), elegibilidad usa `products.hasInStock`, y sugerencias cachean `style_profiles` en memoria (TTL 5 min). Datos: migración `20260225170000_real_style_perf_indexes` con índices parciales para cola pendiente ordenada y agrupación de resumen elegible.
- MC-148 | done | P1 | Curación manual `real_style` en `/admin/real-style` | Nuevo módulo admin 1x1 con UX tipo baraja (1 carta + 2 previews), 8 cajas fijas, asignación inmediata por drag/click/teclado (`1..8`), `Saltar` por sesión y sugerencia visual (sin autoasignar) basada en similitud de tags `style_profiles`. Persistencia en `products.real_style` (nullable, set cerrado de 8) con índice + check constraint, endpoints `GET /api/admin/real-style/queue` y `POST /api/admin/real-style/assign`, y auditoría de cambios en `metadata.enrichment_human` con guardia de concurrencia (`409` si ya fue asignado).
- MC-133 | done | P1 | PLP `/catalogo`: estabilidad real de infinite scroll + precio/histograma móvil determinista + orden determinista paginado | `CatalogoFiltersPanel` calcula `priceBoundsFetchKey` desde estado efectivo en draft mobile y auto-solicita `mode=full` al abrir/editar filtros, con refetch controlado cuando el cache no trae histograma renderizable y placeholder explícito de carga. `PriceRange` elimina `Limpiar/Todos` locales, usa layout fijo de mínimo/máximo y barras más visibles. `CatalogProductsInfinite` migra a cursor secuencial robusto (sin prefetch agresivo), guarda anti-reentrada por página en vuelo y reintenta salto acotado cuando recibe páginas completas 100% duplicadas; mantiene fallback manual visible. Backend conserva desempate determinista por `id` en órdenes paginadas (`catalog-query` + `catalog-data`) y `catalog-data` preserva mínimo de precio real (sin bajarlo artificialmente a `0` por alineación).
- MC-147 | done | P1 | Hero Home con collage responsive por producto | `HomeHeroSlide` ahora incluye `heroImageUrls`; `getHeroSlides` hidrata galería desde `variants.images[]` en una sola consulta, normaliza/deduplica, fuerza cover y ordena determinísticamente por semilla de 3 días (`seed+productId+imageUrl`, pool `8`). `HomeHeroImmersive` migra de imagen única a collage sin gaps con reglas de repetición (`A,A,A,A` / `A,B,A,B` / `A,B,C,A` / `A,B,C,D`) y visibilidad responsive por breakpoint (`<768:1`, `md:2`, `lg:3`, `2xl:4`), manteniendo autoplay/manual y prioridad de carga solo en slide 1/panel 1.
- MC-146 | done | P0 | Corrección sostenible de precios Home (multi-moneda + identidad de producto + Tiendanube ES/CO) | `pricing_config` ahora soporta `fx_rates_to_cop/supported_currencies` con sync `usd_cop_trm`↔`fx_rates_to_cop.USD`; conversión efectiva y SQL migradas a CASE multi-moneda (sin tasa => `null`), panel admin de pricing extiende edición USD/EUR/ARS, upsert de catálogo prioriza `sourceUrl` canónica y evita mezcla por `externalId` ambiguo, adapter `tiendanube` dedicado fuerza canónica ES/CO para `/fr|/us` y selecciona JSON-LD principal por URL; scripts de backfill de rollups y señales actualizados a multimoneda.
- MC-145 | done | P0 | V5 antiatasco + archivo automático seguro en `/admin/catalog-refresh` | Motor de `archive-candidates` con reglas canónicas (`404_real`, `no_products_validated`), tabla de auditoría `brand_archive_events`, aplicación transaccional de archivo (`isActive=false`, cierre de runs/items, limpieza de jobs, productos `archived`), bloqueo `brand_inactive` en run manual, ocultamiento completo en front/user APIs (`/marca/[slug]`, favoritos/listas GET/POST), métricas/alertas de archivo en `state` y acciones `Dry-run/Aplicar` en `CatalogRefreshPanel`.
- MC-144 | done | P0 | Home nunca vacía (hotfix SSR/ISR de módulos críticos) | Se reemplazó fallback crítico `[]` por fetchers resilientes con cadena real para Novedades/Categorías/Foco/PriceDrop/Tendencia diaria, guard de publicación `HOME_CORE_EMPTY` en `HomeBelowFold`, fallback local para `daily_trending` en latencia extrema, estado editorial explícito en `ProductCarousel`, optimización de queries home (`minPriceCop` + window functions), `next.config.ts` con `images.localPatterns` para `/api/image-proxy`, migración de índices `20260224164500_home_core_resilience_indexes` y smoke `npm -C apps/web run smoke:home:core`.
- MC-143 | done | P1 | Home Fase 2: hero 4 slides + categorías carrusel 24 + módulos PriceDrop/Favorites/DailyTrending + dedupe global | Se evolucionó la home con `HomeHeroImmersive` rotativo (4 slides, `74svh/68svh`, autoplay/manual), `CategoryGallery` uniforme tipo carrusel (`24` categorías, 2 filas desktop/1 mobile), `HomeTrendingGrid` ampliado (`24` productos, hasta `12` subcategorías y carga incremental mobile), y `ConversionCoverageBlock` V2. Se integraron `HomePriceDropRail`, `HomeFavoritesRail` y `HomeDailyTrendingRail` después de conversión; se añadió deduplicación global entre módulos mediante `HomeSelectionRegistry` + `collectUniqueProducts`. Datos/infra: funciones home nuevas (`getHeroSlides/getFocusPicks/getPriceDropPicks/getMostFavoritedPicks/getUserFavoritePicks/getDailyTrendingPicks`), tabla Prisma `HomeTrendingDaily`, endpoint cron `/api/admin/home-trending/cron` y cron en `vercel.json`. Robustez media: `image-proxy` endurecido, fallback de logos en `BrandMarquee`, endpoint `/api/home/user-favorites` con fallback anónimo 200, y script `backfill-brand-logos-to-blob.mjs`.
- MC-142 | done | P1 | `/admin/catalog-refresh`: estado único reconciliado + “100% real” verificable + script de reconciliación histórica | Nuevo util `refresh-status` define `deriveCatalogStatus` y `computeOperational100Real`. `GET /api/admin/catalog-refresh/state` agrega `brands[].catalogStatus`, `brands[].statusDiagnostics`, y en `summary` los campos `operationalCoverageExact/operationalHealthOk/operational100Real`, `% raw/display` (sin redondeo falso a 100), `realGapReasons`, `statusMismatchCount` y `statusMismatchSample` manteniendo compatibilidad con `runStatus` + `refresh.lastStatus`. UI `/admin/catalog-refresh` reemplaza columna dual por estado único con tooltip técnico, añade bloque “Estado 100% real” y muestra causas explícitas cuando no aplica. Script nuevo `apps/web/scripts/reconcile-catalog-refresh-status.mjs` (`--dry-run|--apply`, `--days`, `--brandId`) corrige metadata histórica (`lastStatus`, `lastFinishedAt`, `lastError` cuando aplica) y genera reporte JSON en `reports/catalog_refresh_diagnostics/`.
- MC-141 | done | P1 | Optimización urgente Home (P0/P1): streaming + reducción de payload + UX mobile | Home `/` ahora renderiza above-the-fold primero y difiere below-the-fold con `Suspense` (`HomeBelowFold`), se removieron wrappers `RevealOnScroll` de la composición principal, el hero pasó a server-first con imagen LCP optimizada, se recortó carga inicial en `trending/curated` (items/grupos on-demand en mobile), y `proxiedImageUrl` permite bypass directo para dominios soportados por Next (`blob.vercel-storage.com`, `cdn.shopify.com`, `*.myshopify.com`) para aprovechar optimización nativa de imágenes. Validado con build/lint focalizado + Playwright/Lighthouse local.
- MC-140 | done | P1 | Antiatasco V2 por marca (`/admin/catalog-refresh`): reconciliación activa + retries higiénicos + alertas críticas no truncadas | Se endureció la reconciliación para analizar `active` colgados/zombies (`CATALOG_RECONCILE_ACTIVE_HUNG_MINUTES`, `includeActiveAnalysis`, `scanUntilMatchLimit`), con salida `aggressiveRequired/aggressiveReason` y breakdown de zombies en `queue-health`. En refresh semanal se filtró reinyección de `failedRefs` por error transitorio y compatibilidad por plataforma (Shopify/Woo/VTEX), y `finalizeRefreshForRun` ahora persiste `catalog_runs.status='failed'` cuando aplica quality gate. Se reforzó media guardrail: `processCatalogRef` usa `CATALOG_REFRESH_REQUIRE_BLOB_IMAGES` explícito, conserva media Blob existente cuando upload falla y canoniza errores a `blob_required_no_blob_images`. `state` agrega `criticalOperationalAlerts` no truncadas y prioridad crítica antes del `alertLimit`; UI muestra señales de recovery agresivo y status `run/refresh` sin ambigüedad. Además, auto-clear de `manualReview` añade modo relajado seguro (`1 run/45d`, `failedRate<=10%`, sin pendientes).
- MC-139 | done | P1 | Home Fase 1 inspirada: Hero/Categorías/Trending/Marcas V2 + bloque de conversión | Se implementó reemplazo in-place de los módulos de home definidos para Fase 1: `HomeHeroImmersive` V2, `CategoryGallery` V2, `HomeTrendingGrid` (sustituye carrusel de trending) y `BrandMarquee` V2; se añadió `ConversionCoverageBlock` inmediatamente después de Trending con CTA deshabilitado `Próximamente`. Se extendió la capa de datos (`HomeCoverageStats`, `getHomeCoverageStats`, `BrandLogo` enriquecido con `productCount/categoryCount/heroImageUrl`) y `page.tsx` integra la nueva sección manteniendo semilla determinista de 3 días y datos reales. Los agregados de home para marcas/cobertura quedaron restringidos a universo activo (`brands.isActive=true`, productos no archivados), con fallback elegante por sección sin romper layout.
- MC-138 | done | P1 | Antiatasco `/admin/catalog-refresh`: BullMQ endurecida + recuperación agresiva + drift/heartbeat + guardrails de worker | Se corrigió la semántica de recuperación en catálogo/enrichment con helpers explícitos `resetQueuedItemsAll/resetStuckItemsAll` (sin no-op por `olderThanMs=0`), y se reemplazaron call-sites críticos de resume/force/auto-recover. El encolado de catálogo ahora hace top-up por profundidad real por run (`CATALOG_QUEUE_TARGET_DEPTH`, default `max(enqueueLimit, workerConcurrency*3)`) para evitar inflación de backlog. Se añadió reconciliación DB↔Redis (`POST /api/admin/catalog-extractor/reconcile`) con limpieza de jobs huérfanos, normalización de estado y reenqueue controlado; además, `drain` elimina jobs Redis del item procesado para reducir fantasmas. `GET /api/admin/queue-health` incorpora flags `heartbeatMissing/activeHung/queueDriftDetected` + detalle de drift, y `GET /api/admin/catalog-refresh/state` agrega diagnóstico (`diagnostics.queueDrift/heartbeat/progress`) y severidad basada en progreso real reciente. En UI (`/admin/catalog-refresh`) se habilitaron acciones `Reconciliar cola` y `Resume fuerte` desde alertas/guardrails. Worker endurecido: timeout HTTP finito por default (`WORKER_FETCH_TIMEOUT_MS=120000`), fail-fast de heartbeat al boot y alertas explícitas cuando hay `heartbeatMissing` o `activeHung`. Se agregó script de precheck de offset (`scripts/precheck-refresh-timestamp-offset.mjs`) y migración guardada a `timestamptz` con guardrail de abort por offset inconsistente (`20260224120000_catalog_refresh_timestamptz_guarded`).
- MC-137 | done | P1 | FinOps Blob v4 (huérfano acordado + compresión obligatoria + guardrails DB) | Se consolidó la definición de huérfano sobre referencias DB (`products.imageCoverUrl`, `variants.images[]`, `assets.url`, `assets.blobPath`) y se implementó limpieza segura por prefijo/edad en `prune-orphan-blobs.mjs` (`catalog>=14d`, `image-proxy>=7d`, lotes, `maxDelete`, guardrail por error-rate y gate contra `blob-cost-audit-latest.json` con tolerancia `<1%`). Se reforzó `blob-cost-audit.mjs` con `variantsAllExternal/variantsMixed` y métricas `bytes originales vs optimizados` (p50/p90). En escritura, `src/lib/catalog/processor.ts` deja `CATALOG_REQUIRE_BLOB_IMAGES=true` por default, `src/lib/catalog/extractor.ts` bloquea creación con media externa salvo escape hatch, y se agregó migración Prisma `20260223163000_enforce_blob_media_urls` con triggers en `products`/`variants` para impedir URLs no-blob (escape hatch controlado). Se agregó compresión previa obligatoria con `sharp` en `src/lib/media/optimize-before-blob.ts` + integración en `catalog/blob.ts`, `api/image-proxy/route.ts` y backfills (`backfill-image-covers-to-blob.mjs`, `backfill-variant-images-to-blob.mjs`). `image-proxy`/backfills usan `head()` como camino principal y `list()` queda desactivado por default.
- MC-136 | done | P1 | Corrección de precios: COP exacto, USD con redondeo marketing | Se creó `src/lib/price-display.ts` para centralizar regla de display (`COP` sin redondeo marketing; `USD`/override `USD` con redondeo), aplicado en catálogo/home/compare/admin/favoritos/listas; `CatalogoFiltersPanel` pasó a step dinámico de precio y la señal de cambio de precio (`catalog/extractor` + `backfill-product-price-change-signals.mjs`) quedó alineada con la misma semántica condicional.
- MC-114 | done | P1 | Auth Descope: UX sin `Reintentar` para `E061301` + anti-loop de `next` | `SignInClient` resuelve `/sign-in?code=...` con bootstrap + `oauth.exchange` controlado (una sola vez por código) cuando no hay sesión local; `E061301` se maneja como carrera recuperable con poll corto y sin UX de error. Además, `next/returnTo` se sanitiza para bloquear destinos auth (`/sign-in`, `/auth/callback`) y query params OAuth, evitando bucles de login.
- MC-114 | done | P1 | Auth Descope: fix `E108202` (dominio no aprobado) + hardening de diagnóstico en `/sign-in` | Se actualizaron `trustedDomains` del proyecto en Descope (prod + alias `git-main` + localhost), y la UI de login ahora parsea `CustomEvent.detail` para mapear `errorCode` (incluye mensaje explícito para `E108202` con host actual) y log estructurado (`hostname`, `flowId`, `projectId`, `errorCode`, `errorDescription`).
- MC-135 | done | P1 | PLP: indicador/filtro de cambio de precio + filtro de ocasión | Se añadió señal persistida en `products` (`priceChangeDirection`, `priceChangeAt`) calculada por cambio de precio mínimo visible (redondeo marketing), filtro `price_change=down|up` con ventana de 30 días, chip en cards (`↓/↑`) junto al precio, sección “Ocasión” en filtros desktop/mobile vía `facets-lite.occasions`, noindex SEO para `price_change`, índice parcial de soporte y script de backfill inicial desde `price_history`.
- MC-134 | done | P1 | Home inmersivo editorial (SSR + islas client) | Rediseño integral del home `/` con estrategia SSR + islas: hero full-bleed parallax, `RevealOnScroll`, carruseles swipe/drag para Novedades y Trending, galería 3:4 de categorías, `CuratedStickyEdit`, paleta `Shop by color` asimétrica, marquee infinito de marcas y nuevo `HomeProductCard` vertical 3:4. Se añadieron `framer-motion` + `lucide-react`, se creó `src/lib/home-types.ts` para desacoplar tipos de `home-data` (`server-only`) y se mantuvo intacta la semilla determinista de 3 días + fetch server-side.
- MC-133 | done | P1 | Curación programada + señales editoriales (❤️/👑) + sorts PLP | `/admin/product-curation` migra a cola persistente compartida (composer en modal, panel lateral de cola, aplicar pendientes global, aplicar/duplicar/eliminar por item, conflictos potenciales y quick actions ❤️/👑 por card). Backend nuevo: `product_curation_queue_items`, `product_curation_apply_runs`, endpoints `/api/admin/product-curation/queue*`, motor único `apply-engine` reutilizado por `/bulk`. Catálogo público agrega sorts `top_picks` y `editorial_favorites` sin badges públicos.
- MC-133 | done | P1 | PLP `/catalogo`: corrección de filtro de precios (histograma + UX + performance medible) | Histograma de precio orientado a productos (no conteo bruto de variantes) con fast-path por rollups y dedupe por `productId` por bin; fast-path `price-only` (sin `color/size/fit`) en `products-count/products-page/products` usando `products.minPriceCop/maxPriceCop/hasInStock`; `mode=full` de `price-bounds` pasa a fetch bajo demanda en UI (solo cuando el usuario interactúa con Precio); tooltip de slider con edge-clamp en min/max; dock mobile con lock duro + scrim/loading + watchdog hasta navegación estable; benchmark actualizado con desglose `cold/warm` por endpoint/escenario (`base`, `price_min_max`, `price_range`) y validación SLO (`warm <1.2s`, `cold <3s`).
- MC-132 | done | P1 | Header + mega menu: fix ancho achatado + rediseño UX desktop/mobile | Header desktop migra a grid (`auto | minmax(0,1fr) | auto`) con buscador responsive `clamp`; mega menu desktop pasa a estado controlado (hover + pin por click, cierre por outside/Esc, panel compartido a ancho completo del container y scrim único); menu mobile migra a drawer jerárquico por niveles (root/género/sección) con barra sticky y navegación atrás; se agrega telemetría de eventos `menu_open`, `menu_pin_toggle`, `menu_item_click`, `menu_mobile_step`.
- MC-132 | done | P1 | Mega menu UX polish: densidad desktop + reglas de subcategorías | Se compactó el espaciado vertical de líneas en desktop, se eliminó el desglose especial de `ropa_deportiva_y_performance` y se ocultaron subcategorías sin productos (`count=0`) en desktop/mobile.
- MC-130 | done | P1 | Moneda por marca (USD) + TRM editable + redondeo marketing (xxxx0000) | Auto-clasificación diaria de marcas USD (>75% muestra: variantes USD o COP < 1999), override por marca en `brands.metadata.pricing`, TRM configurable en admin (KV `standard_color_config:pricing_config`), UI siempre en COP con conversión USD→COP y redondeo display a múltiplos de 10.000 COP; cron Vercel diario + botón manual en `/admin/pricing`.
- MC-131 | done | P1 | PLP `/catalogo`: sort por precio sin timeout con rollups persistidos | Se agregaron rollups en `products` (`hasInStock`, `minPriceCop`, `maxPriceCop`, `priceRollupUpdatedAt`), script de backfill (`backfill-product-price-rollups.mjs`), fast-path de queries para `price_asc/price_desc` y `price-bounds` (lite) usando `products` cuando no hay filtros de variante, fallback al query pesado cuando sí los hay, e índices concurrentes de soporte para orden por precio (global, por `category` y por `category+subcategory`).
- MC-125 | done | P1 | PLP `/catalogo`: corregir outliers de precio (rangos astronómicos) y estabilizar orden por precio | Se aplicó guardia de precio válido (`CATALOG_PRICE_MAX_VALID`, default 100M COP) en cálculo de bounds, min/max de cards y sort `price_asc/price_desc`; además, `/api/catalog/price-bounds` vuelve a exponer `bounds+stats+histogram` y la ingestión sanea precios fuera de rango para no persistir nuevos outliers.
- MC-125 | done | P1 | PLP `/catalogo`: estabilizar filtro de precio evitando saturación por prefetch de navegación | Se desactivó `prefetch` en links del header/mega menu (desktop + mobile) para evitar ráfagas de requests `_rsc` que competían con transiciones de filtros y dejaban estados “Actualizando…” prolongados.
- MC-125 | done | P1 | PLP `/catalogo`: hardening de filtros al volver de pestaña inactiva + scroll infinito más estable | Se agregó guardia anti-bloqueo en transiciones de filtros (timeout + release en focus/visibility/pageshow), se evitaron `router.replace` no-op en filtros/dock mobile, y se reforzó infinite scroll con timeout/abort en `loadMore/prefetch`, reintento por eventos `focus/online/pageshow` y fallback por proximidad real al sentinel.
- MC-128 | done | P1 | PLP `/catalogo`: debounce de filtros en desktop + `price-bounds` `mode=lite/full` + índices GIN para tags | Desktop: panel de filtros en modo draft con auto-apply (400ms) para seleccionar varios filtros sin múltiples navegaciones. Precio: `/api/catalog/price-bounds` soporta `mode=lite|full` y la UI carga bounds rápido y insights completos lazy. DB: índices GIN parciales para `materialTags/patternTags/seoTags/occasionTags` + script Node para aplicarlos sin `psql`.
- MC-129 | done | P1 | PLP (SEO + filtros): rutas canónicas, facets contextuales, conteos reales y generador SEO (Bedrock) | Canonical PLP `/{femenino|masculino|unisex|infantil}/...` (308 desde `/g/*`), facets-lite contextual (marcas por conteo desc + patrón), chips subcategoría operativos, contador real de marcas, toggle mobile por iconos y panel `/admin/plp-seo` con generación Bedrock persistida en `plp_seo_pages` (runs/items + worker BullMQ).
- MC-016 | done | P1 | ISR/Cache y performance | PLP `/catalogo`: filtros <2s con índices DB (Neon) + hardening UI de subcategorías; benchmark reproducible.
- MC-125 | done | P1 | Catálogo público: refinamientos PLP `/catalogo` (mobile + precio + cards) | Desktop: categorías top 10 con scroll + hint, barra de filtros más angosta, toolbar con iconos (guardar/limpiar) y guardados con delete visible. Mobile: dock fijo + toggle de columnas (1/2) en header + formato fijo 3:4 + ancla arriba; menú mobile con scroll interno (no desplaza la página) y search sin zoom iOS. Precio: histograma y presets más útiles con dominio robusto (percentiles p02/p98) + slider touch + rangos `price_range=min:max` repetibles. Cards: crossfade sin flicker, comparador mobile 1:1, enlaces de producto siempre en nueva pestaña.
- MC-126 | done | P1 | PLP `/catalogo`: polish desktop + listas + SEO + header | Desktop: card de “Filtros” iguala altura con toolbar, columna filtros +20px, scroll por zonas (wheel izquierda mueve filtros), categorías sin scroll anidado (Ver más/menos), toast 6s “Agregar a una lista” al dar fav + drawer para crear/guardar en listas, indicador fijo de “% visto” sobre botón Arriba. SEO: metadata/canonical + `noindex` en filtros profundos; `/buscar` canonical a `/catalogo`. Header: avatar sin texto, “Ingresar” consistente, hover mejorado y glass en menús.
- MC-124 | done | P1 | Taxonomy remap: quitar aprendizaje histórico + batch 10k manual estable | `auto-reseed` elimina la fase de aprendizaje histórico (accepted/rejected) y usa solo señales directas de producto (nombre/descripcion original/metadata/SEO). Se optimizó escritura de propuestas con `deleteMany + createMany` por chunk y se subió `maxDuration` a 300s en rutas de auto-reseed/accept/reject para evitar corridas `running` colgadas. El botón manual mantiene `limit=10000`; ejecuciones no manuales usan límite más conservador (2000) para estabilidad y el modo `force` recupera corridas atascadas de larga duración (`TAXONOMY_REMAP_AUTO_RESEED_FORCE_RECOVER_MINUTES`).
- MC-124 | done | P1 | Taxonomy remap: hardening aprendizaje + estado running auto-reseed en UI | El aprendizaje ahora ignora ruido (solo toma decisiones donde realmente cambió categoría/subcategoría/género), el auto-reseed evita corridas concurrentes con lock persistente (`taxonomy_remap_auto_reseed_runs`, índice único parcial `status='running'`), excluye productos ya revisados para no reproponer, y `/admin/taxonomy-remap-review` muestra feedback visual de ejecución en curso + último resultado de evaluación.
- MC-124 | done | P1 | Taxonomy remap: aprendizaje continuo + auto-reseed por umbral + contador de fase | Nuevo motor `auto-reseed` que aprende de revisiones `accepted/rejected` (nombre/descripcion/SEO), propone cambios para productos enriquecidos y dispara batch automático (hasta 10.000) cuando pendientes ≤ umbral (default 100, con cooldown). Se añadieron endpoints `/api/admin/taxonomy-remap/auto-reseed*`, cron dedicado y métricas de fase en `/api/admin/taxonomy-remap/reviews` para mostrar faltantes en `/admin/taxonomy-remap-review`.
- MC-123 | done | P1 | Cola de revisión manual para remapeo taxonómico (categoría/subcategoría/género) + panel admin | Se creó la tabla `taxonomy_remap_reviews` (pending/accepted/rejected), APIs admin para listar/encolar/aceptar/rechazar propuestas y panel `/admin/taxonomy-remap-review` con foto, razones, confianza y acciones de decisión. Las propuestas SEO-only quedan para revisión humana (no auto-apply).
- MC-122 | done | P1 | Clasificador manual enrichment: cobertura completa categorías/subcategorías + sinónimos ampliados | Se reforzó `signal-harvester` con scoring por coincidencias (no depende del primer match), se incorporó diccionario expandido por categoría y se añadieron reglas por subcategoría para toda la taxonomía publicada (`CATEGORY_OPTIONS`) con expansión de sinónimos ES/EN y desambiguaciones (`collar` camisa vs joyería, `bota` fit de pantalón vs calzado, `body` cosmético vs prenda).
- MC-121 | done | P1 | Product-enrichment: switch runtime a Bedrock Haiku 4.5 (Converse) + visibilidad operativa en admin | Se habilitó el switch real por `PRODUCT_ENRICHMENT_PROVIDER` (OpenAI/Bedrock) sin forzar OpenAI, default recomendado a `BEDROCK_INFERENCE_PROFILE_ID=arn:aws:bedrock:us-east-1:741448945431:inference-profile/us.anthropic.claude-haiku-4-5-20251001-v1:0`, llamada Bedrock migrada a Converse con tool-use y parámetros de inferencia (`top_k`, `latency`, `stopSequences`). Además, `/api/admin/product-enrichment/run` reporta `requestedItems/selectedItems` para validar batches y `/admin/product-enrichment` muestra proveedor/modelo/prompt/schema y selección solicitada vs creada.
- MC-120 | done | P1 | Catalog-refresh: bloquear auto-ejecución de enrichment `new_products` | Hardening para que corridas de `product_enrichment` creadas por `catalog_refresh` con `auto_start=false` no se drenen automáticamente: `/api/admin/product-enrichment/drain` las pausa de forma defensiva (`auto_start_disabled`) y `runCatalogRefreshBatch` también normaliza/pausa corridas heredadas en `processing`. Al reanudar manualmente (`/api/admin/product-enrichment/run` con `resume=true`), la metadata marca `auto_start=true`. Nota: el comportamiento puede habilitarse sin intervención con `CATALOG_REFRESH_ENRICH_AUTO_START=true` (reanuda corridas legacy y crea nuevas en `processing`).
- MC-119 | done | P1 | Product-enrichment: ejecutar nuevo vs reanudar explícito + conteo de batch consistente | El panel `/admin/product-enrichment` deja de auto-reanudar corridas al ejecutar batch/all: ahora esos botones siempre crean corrida nueva (`startFresh`), y se agrega botón explícito para reanudar. El endpoint `/api/admin/product-enrichment/run` toma `startFresh=true` por defecto cuando `resume=false` (compatibilidad con clientes viejos), evitando que se ignore el tamaño seleccionado.
- MC-118 | done | P1 | Hardening product-enrichment (image URLs) + revisión manual visible en admin | Normalización de URLs de imágenes antes de OpenAI (`//` y relativas), descarte de URLs inválidas para evitar `Invalid image_url`, nuevo endpoint `GET /api/admin/product-enrichment/review-items` y mejora de `/admin/product-enrichment` con tabla operativa de revisión manual/baja confianza y acceso directo a producto.
- MC-117 | done | P1 | Reingeniería enrichment v12.5 (señales + prompts por grupo + consistencia) | Harvesting pre-LLM (nombre/descripcion original/metadata vendor), prompts especializados por grupo con routing determinístico (sin segunda llamada), estrategia de imágenes por categoría, validador post-LLM con auto-fix + `confidence` local y `review_required`, preservación de `original_description`, y bloqueo por defecto de re-enrichment IA en productos ya enriquecidos.
- MC-116 | done | P1 | Catálogo público: ocultar no enriquecidos + fix imágenes proxy | `/catalogo` (y aliases `/g/*`, `/buscar`) oculta productos sin `products.metadata.enrichment`; `next/image` evita optimizar `src=/api/image-proxy` (unoptimized) para no caer en 400 `INVALID_IMAGE_OPTIMIZE_REQUEST`; script de backfill de covers a Blob.
- MC-115 | done | P1 | Curación humana (admin) + bulk edit de características | Nuevo módulo `/admin/product-curation` con filtros compartidos con `/catalogo`, scroll infinito, selección multi-producto y bulk edit modal (`replace/add/remove/clear`) con auditoría en `metadata.enrichment_human` sin tocar `metadata.enrichment`. Hardening 2026-02-16: universo por defecto alineado con `/catalogo` (`enrichedOnly=true` + `inStock=true`) en `products/facets/ids`.
- MC-127 | done | P1 | Curación humana (admin): filtro por SEO tags (top 300) | Caja de filtros con SEO tags abundantes (hasta 300 principales) para acelerar la curación de productos.
- MC-114 | done | P1 | Auth usuarios externos (Descope) + perfil + favoritos/listas + analytics base | Login social con Descope, perfil privado, tablas y endpoints de experiencia/favoritos/listas.
- MC-009 | done | P1 | Taxonomía editable + publicación (admin) | Nuevo panel `/admin/taxonomy` con draft→publish, tabla `taxonomy_snapshots`, endpoints admin, propagación a prompt enrichment, curación humana y labels de facets, editor de `style_profiles` + recompute.
- MC-113 | done | P1 | Home editorial + mega menu + rotación 3 días | Home público con mega menu por género, reglas category+subcategory y rotación automática cada 3 días.
- MC-112 | done | P1 | Des-dockerizar repo | Eliminar Dockerfiles/compose y actualizar docs para ejecución local con npm.
- MC-111 | done | P1 | Paleta 200 + matching estandarizado de combinaciones | `color_combinations.colorsJson` + paleta 200 en `color_combination_colors`; matching 200→60 con standard colors y rebuild de relaciones.
- MC-108 | done | P1 | Fix cola de scraping de marcas | Encolar solo marcas sin job completed y drenar cola existente desde UI.
- MC-109 | done | P1 | Onboarding de marca en modal | Crear marca inicia pipeline (enrich → tech → catálogo → productos) con barra de progreso y endpoints onboarding.
- MC-110 | done | P1 | Refresh semanal de catálogo + monitor admin | Cron semanal con jitter, refresco completo, métricas de cambios y panel `/admin/catalog-refresh`; VTEX discovery sin cap fijo (capping opcional por env); hardening: normalización LLM con fallback, runs de enrichment en `paused` (sin auto-ejecución), concurrencia por marca configurable (`CATALOG_REFRESH_BRAND_CONCURRENCY`) y fix de parseo en `/api/admin/catalog-refresh/cron` para que parámetros opcionales ausentes no degraden a `maxBrands=1`/`brandConcurrency=1`/`maxRuntimeMs=5000`. Actualización 2026-02-23: gate de éxito operativo (`CATALOG_REFRESH_MAX_FAILED_ITEMS=30`, `CATALOG_REFRESH_MAX_FAILED_RATE=0.10`), backoff exponencial por marca fallida (`CATALOG_REFRESH_FAILED_BACKOFF_BASE_HOURS=6`, `CATALOG_REFRESH_FAILED_BACKOFF_MAX_HOURS=72`), KPI dual en `/api/admin/catalog-refresh/state` (cobertura operativa vs calidad) y script one-shot `apps/web/scripts/rebaseline-catalog-refresh-metadata.mjs`. Actualización 2026-02-23 (alerts/UI): `state` agrega `schedulerDue/operationalOverdue/lastOperationalAt`, `operationalMissingBrands`, `oldestOperationalRefresh`, filtro opcional `includeEnrichmentAlerts=true`; la UI migra a campanita desplegable (sin alertas enrichment por default), bloque de faltantes al 100% y estado de jobs. Actualización 2026-02-23 (force+autorrecovery): nuevo `POST /api/admin/catalog-refresh/force` asíncrono (evita 504 en force por marca), mini barra por marca (`runProgress`) en faltantes, alertas `stale_auto_recovering/stale_no_refs`, auto-remediación programada (`CATALOG_REFRESH_AUTO_REMEDIATE_*`) y auto-conversión `manualReview` por evidencia (`CATALOG_MANUAL_REVIEW_AUTOCLEAR_*` + script `apps/web/scripts/auto-clear-manual-review.mjs`). Actualización 2026-03-01: cap global de runs activos (`CATALOG_REFRESH_MAX_ACTIVE_RUNS`), auto-reconcile pre-batch (`CATALOG_REFRESH_AUTO_RECONCILE*`) para drift DB↔BullMQ, payload operativo expandido en `/api/admin/catalog-refresh/cron` (`activeRunsBefore/activeRunCap/throttledByActiveCap/autoReconcile`) y semántica de stale ajustada en admin (`completed_stale/unknown` con alias `no_status`). Actualización 2026-03-02: timeout HTTP del extractor endurecido (defaults `CATALOG_HTTP_TIMEOUT_MS=45000`, `CATALOG_PDP_HTML_TIMEOUT_MS=45000`), deploy prod `dpl_2SoqagxkHjaHNMEnA8NnpEZaDFNy` y monitoreo post-release con heartbeat estable, drift `waitingItemNotQueued` a 0 vía auto-reconcile/cron y cobertura operativa en `338/534` mientras continúa remediación de runs históricos `processing`. Actualización 2026-03-02 (ops recovery): se aisló run en tormenta de abortos (`yerbabuena`, `f2742252-e8f0-43db-8e0c-6007e2252ed5`) con `catalog-extractor/stop`, se relanzó worker persistente con `CATALOG_WORKER_CONCURRENCY=8` y se ejecutó reconcile continuo; resultado en ventana corta: `waitingCatalog -31` (2 min), `lastCompletedAt.catalog` avanzando y `waitingItemNotQueued=0` estable. Actualización 2026-03-02 (estabilización balanceada): `state` agrega agregación top N de alertas `stale_processing_no_progress` con overflow + detalle técnico completo (`summary.processingNoProgress*`, `diagnostics.processingNoProgress`), severidad dinámica de drift (`danger` solo con `waitingItemNotQueued>0` o `activeZombieCriticalCount>0`), gracia de `item_completed` en active (`CATALOG_QUEUE_ACTIVE_COMPLETED_GRACE_SECONDS`), endpoint nuevo `POST /api/admin/catalog-refresh/remediate` (dry-run/apply balanceado), cron con bloque `stuckRemediation`, y script operativo `apps/web/scripts/monitor-catalog-refresh-ops.mjs` para monitoreo + auto-acciones. Actualización 2026-03-02 (rollout prod directo): deploy `dpl_HjMgR8euZovhhNLAqXJreDBMcikX` a alias `https://oda-moda.vercel.app`, dry-run/apply balanceado ejecutado (`resumed=30`, `paused=30`, `requeued=3000`, `errors=0`), monitor operativo activo con snapshot `reports/catalog_refresh_diagnostics/2026-03-02T03-32-14-983Z-catalog-refresh-ops.json`, y reconcile reforzado (`jobScanLimit=2500`, `reenqueueLimit=800`, `includeActiveAnalysis=true`) estabilizando `waitingItemNotQueued=0` y `activeZombieCriticalCount=0` en la verificación post-acción. Actualización 2026-03-02 (anti-rebote): `runCatalogRefreshStuckRemediation` ejecuta post-reconcile automático al finalizar (`postReconciled/postReconcileResult`) y el monitor añade cooldown de remediación (`--remediate-cooldown-minutes`, env `CATALOG_REFRESH_MONITOR_REMEDIATE_COOLDOWN_MINUTES`, default 30) para evitar requeue masivo por ciclos consecutivos. Ajuste operativo validado en prod `dpl_F9JBYh2iomy5fkAw73zqCEhyxxv5`: ventana de 15 min con reconcile reforzado y cooldown mostró drift crítico sostenido en 0 (`waitingItemNotQueued=0`, `runsRunnableWithoutQueueLoad=0`, `activeZombieCriticalCount=0`) tras el primer ciclo correctivo. Hardening adicional en prod `dpl_9hXDSfrqKqxpZmT9e2QmBkgy5f6y`: monitor con timeout por request (`--request-timeout-ms`, env `CATALOG_REFRESH_MONITOR_REQUEST_TIMEOUT_MS`, default `45000`) y reconcile reforzado aplicado nuevamente (`waitingItemNotQueued 127 -> 0`, `activeZombieCriticalCount=0`). Actualización 2026-03-02 (deploy fix definitivo): se eliminó `apps/web/middleware.ts` (causaba fallos intermitentes de publish en Vercel), se fijaron regiones `iad1/gru1` en `apps/web/vercel.json`, y el deploy Git final quedó Ready en `dpl_8d6M3SxQbBdr63CTc4HBLxLuuVzd` (alias `https://oda-moda.vercel.app`). Verificación post-deploy: workers online, `staleBreakdown` corregido (`processing=47`, `paused=102`), `processingRunsWithoutRecentProgress=42` y drift crítico sin `waitingItemNotQueued`. Actualización 2026-03-02 (cap fix): se endureció parseo de `CATALOG_REFRESH_MAX_ACTIVE_RUNS` en `refresh.ts` para tolerar envs con secuencias `\\n`, se aplicó fix de compatibilidad de tipos de drift en remediación, y el deploy final Ready `dpl_Fki1S2234VGcmWvHfz4o1MXeLYDX` confirmó `activeRunCap=24` en `/api/admin/catalog-refresh/state`. Actualización 2026-03-02 (autónomo 99%): `runCatalogRefreshStuckRemediation` incorpora estrategia `aggressive_tail_close` con cierre terminal seguro de cola (`tailProgressPct`, `tailMaxRemaining`, `tailStaleMinutes`, `forceTerminalizeRemaining`), `runCatalogRefreshBatch` aplica capacidad por `processing` real y auto-finalize también en `mode=light`, `GET /api/admin/catalog-refresh/state` expone `highProgressNoProgress*` + `processing/paused/blocked/schedulingCapacityRemaining`, `POST /api/admin/catalog-extractor/drain` agrega `dryRun`/micro-drain con lock y contadores de cierre forzado, y `monitor-catalog-refresh-ops.mjs` queda 24/7 real con `iterations=0`, lock de instancia única, estado persistido y guardrail de heartbeat (`drain + reconcile + cron light`) en ciclos consecutivos. Actualización 2026-03-02 (diagnóstico amplio y baja intervención): monitor endurecido para leer `diagnostics.processingNoProgress.all` (no solo top), calcular persistencia por run entre ciclos (`consecutiveNoProgressCycles`), generar `noProgressInsights` (buckets de avance/antigüedad + top persistentes), tolerar caídas parciales de endpoints (`state`/`queue-health`) sin abortar snapshots, y limitar remediación en modo `minimal` a runs cercanos a cierre que persisten >= `CATALOG_REFRESH_MONITOR_NEAR_CLOSE_MIN_CONSECUTIVE_CYCLES`.
- MC-110 | done | P1 | Seguimiento continuo de estabilización (2026-03-02 tarde) | Remediación balanceada ahora resume también runs `paused/blocked` reanudables (prioriza `paused(stuck_over_cap)`), el cálculo de `pauseOverCap` usa `processing` real para no pausar de más cuando hay backlog pausado, y el cron puede disparar remediación si existe cola `paused_over_cap` aunque no supere el umbral de `processingNoProgress`. Deploys prod: `dpl_7djCkYbAftRrLu8iXmc9HCRs56Rs` + `dpl_FewGWfxjzb2gSWVZGGVbP2Azoc5L` (alias `https://oda-moda.vercel.app`). Validación inmediata: `paused(stuck_over_cap) 121 -> 82`, `processing 38 -> 77`, drift crítico estable en `0` (`waitingItemNotQueued=0`, `runsRunnableWithoutQueueLoad=0`, `activeZombieCriticalCount=0`). Monitoreo: `monitor-catalog-refresh-ops.mjs` persiste cooldown/estado con `--state-file` (`CATALOG_REFRESH_MONITOR_STATE_FILE`) y confirma `remediate_skipped_cooldown` en ejecuciones secuenciales. Ajuste fino adicional en prod (`dpl_Eqrao4Kjb7hhqF9e9iD7Czvd9pxK`): `resumeLimit` queda acotado por headroom de `processing` (`pauseOverCapTarget - processingRunCount`), evitando sobre-resume cuando `processing` ya está alto; verificación directa tras cron: `processing 96 -> 69` y `paused 63 -> 90` en un ciclo (`resumed=0`, `paused=30`).
- MC-110 | done | P1 | Hotfix desbloqueo 24/7 (cron adaptable + monitor agresivo) | Se implementó `mode=light|heavy` en `runCatalogRefreshBatch` y `GET /api/admin/catalog-refresh/cron` (default `light`), con `maxDuration=300` en route para evitar timeout recurrente de 60s en cron; `vercel.json` agenda `light` cada 5m y `heavy` cada 60m. Se publica/asegura en deploy `POST /api/admin/catalog-refresh/remediate` y se endurece `apps/web/scripts/monitor-catalog-refresh-ops.mjs` con parámetros agresivos (`limit=60`, `minNoProgressMinutes=20`, `pauseOverCapTarget=64`), guardrail por `activeHung/zombieCritical` en ciclos consecutivos, reconcile reforzado (`jobScanLimit=2500`, `reenqueueLimit=800`) y estado persistente en `reports/catalog_refresh_diagnostics/monitor-state.json` para operación continua en runner.
- MC-110 | done | P1 | Política de autocierre autónomo para colas near-complete | Hotfix 2026-03-02: cierre automático de runs estancados en alto progreso sin intervención manual. `aggressive_tail_close` pasa a defaults `tailProgressPct=95`, `tailStaleMinutes=60`, `tailMaxRemaining=0` (sin tope), y elimina bypass por `runId` para respetar ventana de stale también en remediación dirigida. `state` baja `highProgress` default a `95` para visibilidad operativa consistente. Monitor alinea defaults (`near-close-progress=95`, tail `95/0/60`) para que no fuerce cierres prematuros.

## Done (2026-01)
- MC-107 | done | P1 | Enrichment: esquema estricto + repair/chunking Bedrock | 1 imagen por variante, repair pass y chunking por variantes; smoke test Bedrock; concurrencia 40 en envs prod.
- MC-106 | done | P1 | Estilos principal/secundario por styleTags | Tabla style_profiles + trigger + backfill; labels humanos en admin.
- MC-105 | done | P1 | Filtro por rol de color en combinaciones | `/api/admin/color-combinations/[id]/products` filtra por categorias permitidas segun rol (dominante/secundario/acento).
- MC-104 | done | P1 | Enrichment productos con Claude (Bedrock) | Product enrichment usa Bedrock (Claude Sonnet 4.5) vía inference profile; OpenAI queda para el resto.
- MC-103 | done | P1 | Campos SEO en productos + enriquecimiento IA | Nuevas columnas seoTitle/seoDescription/seoTags[] y prompt de enrichment genera meta title/description + tags.
- MC-102 | done | P1 | Enriquecimiento productos: persistencia + progreso realtime + reset batches | URL state, conteos por status en UI y endpoint para limpiar batches activos.
- MC-100 | done | P1 | Filtro de categorias + orden por productos en /admin/brands | Multiselect `brands.category` y sort `productCount` asc/desc.
- MC-099 | done | P1 | Contador pendientes marcas: desglose y elegibilidad | `/api/admin/brands` expone pending breakdown (queued/no_jobs/failed/manualReview/cloudflare) y la UI lo muestra.
- MC-098 | done | P1 | Acelerar serving de imágenes (proxy+cache) | Nuevo `/api/image-proxy` cachea en Blob/CDN y el admin lo usa en logos/grids con `next/image`.
- MC-097 | done | P1 | Persistencia de navegación en admin (page/filter en URL) | `/admin/brands` y `/admin/products` conservan página/filtros tras reload/acciones y ajustan página si se sale de rango.
- MC-096 | done | P1 | Modal marcas: stats reales + preview + delete cascada | Modal usa stats reales desde variants, preview 10 productos con deep-link a `/admin/products?productId=...` y delete hard cascade.
- MC-095 | done | P1 | UI: ver marcas sin run agrupadas por tecnología | Toggle en extractor + API soporta onlyNoRun/all y límite ampliado.
- MC-094 | done | P1 | Sitemap discovery tolerante a fallos | Robots/sitemaps con fetch error no bloquean el run; fallback a adapter.
- MC-093 | done | P1 | Sitemap completo sin corte temprano | No se corta en el primer sitemap de productos; límite configurable y sin cap fijo.
- MC-090 | done | P1 | Sitemap budget + precios Woo fallback | Prioriza robots y limita tiempo de discovery; precios Woo se completan con fallback HTML cuando API devuelve 0.
- MC-091 | done | P1 | Drain finaliza runs idle + control sitemap/queue | Drain marca run completed si no hay pendientes; /run respeta enqueueOnly y `CATALOG_FORCE_SITEMAP`.
- MC-092 | done | P1 | Reducir reintentos de catálogo | Máximo 1 reintento por producto y 1 reintento en cola.
- MC-089 | done | P1 | Concurrencia alta + progreso frecuente en extractor | Drenado más rápido y polling ~2s en UI.
- MC-088 | done | P1 | Catalog extractor no pausa por errores de producto | Errores soft no cuentan para auto-pause; continua la corrida.
- MC-087 | done | P2 | Modal productos + carrusel en cards | Colores visibles con swatches, resumen de variantes y navegación de fotos en listado.
- MC-086 | done | P2 | Progreso incremental en panel product-enrichment | Run no drena en la misma request; barra avanza vía polling + cron.
- MC-085 | done | P2 | Style tags exactos 10 | Prompt y validación ajustados a 10 tags exactos.
- MC-084 | done | P2 | Auto-refresh solo con runs processing | Panel enrichment refresca solo cuando hay run activo.
- MC-083 | done | P1 | Estabilidad product-enrichment (cron + auto-refresh) | Drain serverless y cron Vercel para product-enrichment; auto-refresh en panel.
- MC-082 | done | P1 | Normalizacion determinista v2 (reglas extendidas) | Reglas de categoria, material, patron, fit y color mejoradas para reducir LLM.
- MC-081 | done | P1 | Catalog extractor: normalizacion determinista + menos OpenAI | Shopify/Woo sin LLM, payload LLM recortado, retries/backoff en cola, auto-finish marcas sin fallos.
- MC-080 | done | P1 | Enriquecimiento IA de productos | Admin + cola para enriquecer categorías/tags/colores/fit con GPT-5 mini.
- MC-073 | done | P1 | Drain serverless catálogo (Vercel cron) | Endpoint drain + cron cada minuto para procesar sin worker persistente.
- MC-072 | done | P1 | Redis + cola operativa en Vercel | REDIS_URL correcto en Vercel, guardas de cola y reanudacion sin timeouts.
- MC-071 | done | P1 | Blob robusto: sanitizar path + tolerar fallos | Evita error por # y no aborta por fallos parciales.
- MC-070 | done | P1 | Concurrencia catálogo v2 (cola + runs/items) | Tablas + BullMQ + worker + backfill.
- MC-069 | done | P1 | Robustez extractor: telemetría + pausa por errores | lastUrl/lastStage/errorSamples y pausa por errores consecutivos.
- MC-068 | done | P1 | Resume UI + barrido + GPT-5 mini en productos | Botón Resume en pausa/stop, sync cursor, modelo barato para PDP/normalización.
- MC-067 | done | P1 | Detener extractor conserva estado | Stop mantiene cursor/refs y permite reanudar sin reiniciar.
- MC-066 | done | P1 | Finalizar marca en catalog extractor | Botón y endpoint para sacar marcas de la cola y registrar `catalog_extract_finished`.
- MC-064 | done | P1 | Normalizar ImageObject en blob | Extrae contentUrl/thumbnail antes de upload.
- MC-065 | done | P1 | LLM PDP fallback + limpieza de marcas | Clasifica PDP con LLM, extrae RawProduct y elimina marcas no procesables.
- MC-063 | done | P1 | Blob: retry con referer/UA | Reduce fallos por hotlinking al subir imágenes.
- MC-062 | done | P1 | Custom: evitar URLs no-producto desde sitemap | Excluye /portafolio y no usa URLs no-producto cuando no hay coincidencias.
- MC-061 | done | P1 | Unknown: sitemaps extra + inferencia rápida plataforma | Nuevos sitemaps (wp/products/store), inferencia sin LLM y pistas microdata.
- MC-060 | done | P1 | Custom: excluir listados por og:type/rutas | Negativos blog/collections + og:type website sin price meta.
- MC-059 | done | P1 | Custom: omitir listados y detectar producto por pistas | Ajusta heurísticas /tienda y /shop; acepta product hints cuando no hay JSON-LD.
- MC-058 | done | P1 | Mejoras unknown: tech profiler + custom discovery | Tiendanube/Wix + parked/unreachable; patrones /product-page y /product-.
- MC-057 | done | P1 | Marcar manualReview cuando no hay productos | Bloquea runs sin productos y setea manualReview + metadata de revisión.
- MC-056 | done | P1 | Filtrar sitemap a mismo dominio | Evita URLs externas en VTEX y reduce raw vacío.
- MC-055 | done | P1 | Fallback a API si sitemap no trae productos | Evita URLs no-producto en VTEX; usa discovery del adapter.
- MC-054 | done | P1 | Sitemap scan completo + fallbacks Woo/VTEX | Descubrimiento product-aware, gzip/index, límite de sitemaps y smoke test; Vercel pendiente (sin token).
- MC-053 | done | P1 | Fix VTEX linkText en sitemap + error más claro | Deriva linkText desde URL y mejora mensaje cuando raw es vacío.
- MC-052 | done | P1 | Errores visibles en catalog extractor | Muestra último error, bloqueos y errores recientes en panel.
- MC-051 | done | P1 | Catalog extractor por tecnologia con play/pause/stop | Seleccion por plataforma, auto‑avance de marcas, sitemap-first y reanudacion por cursor.
- MC-050 | done | P1 | Fix Unicode tech profiler | Sanitiza Unicode y parseo JSON para evitar errores de escape al guardar metadata.
- MC-049 | done | P1 | Volver a modelo OpenAI gpt-5.2 | Default en scrapers/normalizer y env example/documentación.
- MC-048 | done | P1 | Evidencia textual limpia en scraper de marcas | Limpieza de HTML→texto, prioriza líneas relevantes y reduce ruido en evidencia OpenAI.
- MC-047 | done | P1 | Cambiar modelo OpenAI a gpt-5-mini | Reduce costos en scrapers (marcas, tech, catálogo).
- MC-046 | done | P1 | Reglas de moneda + reset catálogo | Parsing precios, currency en productos, truncate products/variants.
- MC-045 | done | P1 | Progreso extractor productos | Barra de progreso informativa + reanudación por tandas + hardening OpenAI.
- MC-044 | done | P1 | Directorio de productos admin | Cards con detalle, filtros por marca, endpoints productos.
- MC-043 | done | P1 | Catalog extractor por tecnología | Adaptadores Shopify/Woo/Magento/VTEX/Custom, normalización OpenAI, imágenes a Blob, panel admin.
- MC-042 | done | P2 | Revisión manual de marcas | Check azul en cards + toggle en modal, persistido en DB.
- MC-041 | done | P1 | Tech profiler de marcas | Campo ecommercePlatform, perfilado Shopify/Woo/Magento/VTEX, panel /admin/brands/tech.
- MC-035 | done | P1 | Scraper de marcas (enriquecimiento OpenAI) | Panel /admin/brands con cola 1/5/10/25/50; endpoints admin + cron; OpenAI web search JSON mode + fallback HTML; diff de cambios en historial; actualización de tabla brands y metadata.
- MC-006 | done | P1 | Autenticación y roles base | Login admin (email/password + cookie), ruta /admin sin /admin/login, seed admin en Neon, middleware sólo en /api/normalize.
- MC-004 | done | P0 | Conexión OpenAI GPT-5.2 JSON mode | Helper con retries/validación Zod, endpoint `/api/normalize`, middleware Bearer (ADMIN_TOKEN/NEXTAUTH_SECRET), carpeta `/admin` base; README documentado.
- MC-003 | done | P1 | Esquema Neon + migraciones | Prisma schema con brands/stores/products/variants/price&stock history/assets/events/taxonomy/announcements/users; pgvector habilitado; migración `20260115125012_init_schema`.
- MC-002 | done | P1 | Docker compose local (retirado 2026-02) | Servicios web/scraper/worker, Postgres pgvector, Redis; healthchecks; puerto host 3080; `.env.example` alineado con Vercel; stack sube con `docker-compose up -d`.
- MC-001 | done | P1 | Estructura base repo y convenciones | Next app TS/Tailwind en apps/web, estructura servicios scraper/worker, .gitignore, .env.example, docker-compose base, README creado.
- MC-036 | done | P1 | Directorio de marcas admin | Grid 3x5, modal detalle, CRUD marcas, filtros/paginación en /admin/brands; endpoints CRUD en /api/admin/brands.
- MC-037 | done | P1 | Resiliencia scraping admin | Re‑encolar jobs atascados, auto‑resume tras recarga, separación de /admin/brands y /admin/brands/scrape con menú lateral.
- MC-038 | done | P2 | Admin layout anclado | Sidebar anclado a la izquierda para maximizar el canvas de contenido.
- MC-039 | done | P2 | Mejoras cards marcas | Mostrar logo en card y URLs cliqueables en modal.
- MC-040 | done | P1 | Re‑enriquecimiento por marca | Método 2 (14 fuentes, 20k chars) desde card con progreso mini.

---
**Instrucción operativa**: al abordar cualquier historia de este backlog: (0) pedir requisitos previos (credenciales/API keys, definiciones faltantes), (1) levantar servicios locales necesarios (web/scraper/worker) y revisar logs, (2) push a la rama, (3) esperar y revisar build en Vercel hasta que finalice bien, (4) actualizar README con cambios relevantes, (5) marcar la historia como terminada en `USER_STORIES.md`, `BACKLOG.md` y en `STATUS.md`.
